export interface Branding {
  logo: {
    src: string;
    alt: string;
  };
}

export interface SiteConfig {
  name: string;
  Title: string;
  description: string;
  image: {
    src: string;
    alt: string;
  };
}